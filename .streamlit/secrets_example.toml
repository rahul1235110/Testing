# Example secrets file for Streamlit Cloud
# Rename to .streamlit/secrets.toml locally or add these as secrets in Streamlit Cloud

# API keys
AGROMONITORING_API_KEY = "your_api_key_here"

# Database configuration using Streamlit's built-in storage
# This is for saving user data between sessions
[database]
# These will be automatically populated by the application
users = "[]"
field_data = "[]"
irrigation_records = "[]"